<%= link_to attendances_path(event_id: @event.id), method: :post,
            class: "btn btn-#{attending_class} btn-event pull-right attend-btn",
            style: 'margin-left: 8px' do %>
    <i class="fa <%= attending_icon %>"></i> <%= attending_text %>
<% end %>